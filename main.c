// C program to detect Operating System

#include <stdio.h>
#include <dirent.h>
#include "apue.h"

// Driver Code
int main(int argc, char *argv[])
{

    DIR *dp;
    struct dirent *dirp;
    if(argc != 2)
        err_quit("usage: ls directory_name");

    return 0;
}